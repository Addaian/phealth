# Changelog

2026-05-13 — Reviewed Phase 1 PRD; resolved Q1–Q3, defined DSAP, aligned §5.1 gap list with §6.3, added submission-contract section, flipped status Draft → Approved.
2026-05-13 — M0: wrote app/synthetic/persona.yaml (single source of truth); added generate_chart_text.py + chart_text/ paste-into-SP outputs.
2026-05-14 — M1: built SimplePractice [BPS]/[SOAP]/[DAP]/[DSAP] templates (21-section BPS). M2: authored Marcus Reyes chart, exported to data/synthetic_export/.
2026-05-14 — M4: scaffolded FastAPI app, docker-compose (Postgres 16 + pgvector), Alembic, app/ skeleton, /health endpoint — docker+alembic+pytest acceptance green, ruff clean.
2026-05-14 — M5: defined 7-table SQLModel schema (FHIR-shaped, char-offset provenance, pgvector embedding, generated tsvector FTS); first Alembic migration + DB-backed schema tests, all green.
2026-05-14 — Added persona-consistency and chart-text-generator tests ahead of M6; suite now 13 tests (gracefully skips DB-backed tests when Postgres is down).
2026-05-14 — Simplify pass on M0–M5 code: renamed single-letter variables, extracted _render_substance helper, added missing docstring + EMBEDDING_DIM cross-ref comment. Output byte-identical, all 13 tests green.
2026-05-14 — M6: built the full ingestion pipeline — pdf_parser, vcard_parser, section_detector, scale_extractor, entity_tagger, asam_evidence_index, tjc_coverage_matrix, embeddings, provenance, orchestrator + POST /ingest endpoint; ASAM + TJC seed catalogs. All M6 acceptance criteria pass against the real export (4 docs, 7 scales, 6 ASAM dims, 5/5 intentional gaps detected); idempotent re-ingest; HTTP endpoint verified through the docker stack; 15 tests green.
2026-05-14 — M6 sanity check: added explicit `bps is None` guards in tjc_coverage_matrix rules (mypy now clean across all 12 ingest modules); ruff + 15 tests still green.
2026-05-14 — M7: FHIR R4 mappers (app/fhir/mappers.py — Patient/Encounter/DocumentReference/ClinicalImpression/Observation via fhir.resources.R4B) wired into the ingester; GET /patients/{id}/fhir-bundle transaction Bundle endpoint. Bundle round-trips through fhir.resources, unique fullUrls, 1+4+4+3+9 resources; added the pydantic mypy plugin; ruff + mypy clean; 19 tests green.
2026-05-14 — M8: read API surface — GET /patients/{id}, /intake, /timeline, /observations, /asam-evidence, /tjc-coverage (shared deps + Pydantic read schemas). Generated examples/marcus_reyes.json sample artifact. All 9 routes verified through the docker stack; /docs renders; 26 tests green.
2026-05-14 — M9: golden tests for section_detector + scale_extractor, dedicated provenance round-trip test (PRD §5.5), face-validity checklist (date-stamped). 42 tests green.
2026-05-14 — M10: wrote README (persona, why-not-wrap, ASCII architecture, design novelty, run instructions, G1–G5 gap catalogue, production prerequisites, Plan B). Phase 1 complete — phase-gate passed: ruff + mypy clean, 42 tests, clean docker run from the README's exact commands, all PRD §8 acceptance metrics met live.
2026-05-15 — Phase 2 kickoff: wrote documents/phase_2.md (playbook), phase_2_PRD.md (approved; Q1–Q4 resolved), and phase_2_implementation_plan.md (M0–M13). Phase 2 = dual-surface read API (/api/v1/* ergonomic + /fhir/* strict R4), canonical GET /chart with provenance round-trip baked into every observation, audit-on-read, ETag, RFC 7807/OperationOutcome.
2026-05-15 — M0 (Phase 1 carry-over): fixed _fhir_datetime to attach America/Chicago (live FHIR output now carries -05:00 CDT, not Z); added GET /patients list endpoint; tests/test_security.py rejects missing + wrong X-API-Key on /ingest. 44 tests green.
2026-05-15 — M1: moved every patient-scoped read under /api/v1/patients; X-API-Key required on every /api/v1/* and /fhir/* route; added /api/v1/capabilities self-describe (auth-free); api_client fixture also overrides require_api_key. test_fhir.py refactored to api_client. Old /patients/* paths return 404; 46 tests green.
2026-05-15 — M2: audit-on-read dependency — every successful GET on /api/v1/* and /fhir/* schedules one AuditEvent(action='read') via BackgroundTasks (own session, off the response critical path). 401s short-circuit before audit, so unauthorized requests do not pollute the log. tests/test_audit.py covers both invariants. 48 tests green; ruff + mypy clean.
2026-05-16 — M3: unified exception handlers (app/api/errors.py) — single dispatcher branches on request.url.path so /api/v1/* (and unprefixed) emit RFC 7807 application/problem+json and /fhir/* emit FHIR OperationOutcome application/fhir+json. Covers HTTPException (our raises + Starlette's framework 404s) and RequestValidationError (422 path/query coercions). tests/test_errors.py — 6 cases across 404/401/422 × both envelopes, including the /fhir unmounted-path branch. 54 tests green; ruff + mypy clean.
2026-05-16 — M4: ETag + If-None-Match (app/api/etag.py) — Starlette BaseHTTPMiddleware hashes the rendered response bytes with xxh64 and attaches a weak ETag to every JSON GET 200; matching If-None-Match short-circuits to 304 with no body. Streaming media types (NDJSON, octet-stream) and 4xx/5xx are skipped. Duck-types on body_iterator since Starlette's internal _StreamingResponse is not a subclass of public StreamingResponse. xxhash>=3.4 added to pyproject. tests/test_etag.py — 5 cases (header present, stability, 304 short-circuit, mismatch returns 200, errors not ETagged). 59 tests green; ruff + mypy clean.
2026-05-16 — M5: Pydantic schemas refactor + provenance shape — added ProvenanceRead {document_id, char_start, char_end, snippet} canonical envelope; SectionRead grew SectionStatus = Literal["found","looked_but_missing","not_assessed"] (default "found") and optional text/provenance; new ScaleRead with mandatory provenance; ObservationRead / AsamEvidenceRead / TjcCoverageRead gained optional nested provenance. New app/api/fhir_schemas.py base (camelCase alias generator, populate_by_name) ready for M8. tests/test_schemas.py — 10 unit cases on the new shapes. All Phase 1 schemas remain backwards-compatible (new fields default-Noned). 69 tests green; ruff + mypy clean.
2026-05-16 — M6: trinary completeness — new app/api/completeness.py with hard-coded BPS_INTAKE_TEMPLATE (21 sections verified against ingested chart), progress-note templates, classify_section / classify_all, and the `|found| / (|found|+|looked_but_missing|)` score (not_assessed excluded). IntakeRead gained completeness_score; /api/v1/patients/{id}/intake now returns per-section trinary status + score, surfacing template sections that the chart didn't include as looked_but_missing placeholders. tests/test_completeness.py — 13 cases (pure classifier unit tests + two endpoint integrations: Marcus scores 1.0 perfect; one blanked section → 20/21). 82 tests green; ruff + mypy clean.
2026-05-16 — M7 (Task 2 acceptance milestone): canonical GET /api/v1/patients/{id}/chart — new app/api/chart_schemas.py (ChartRead/ChartMeta/ChartPatient/ChartIntake/TimelineNote/ChartObservation/AsamSummary/TjcCoverageSummary, matching PRD §6.1 verbatim) + new handler in app/api/patients.py. Six row-store queries assemble the consolidated response: nested patient.name+identifiers, intake bound to LOINC 11488-4 Consultation Note (21 sections w/ trinary status + scales w/ mandatory provenance), timeline of progress notes bound to LOINC 11506-3 ordered oldest-first, LOINC-only observations with char-offset provenance whose snippets round-trip raw_text[char_start:char_end] exactly, ASAM evidence counts per dimension (severity is honestly null — Phase 3 territory), TJC coverage summary with the 5 intentional gap EPs. tests/test_chart.py — 12 cases covering shape, LOINC bindings, ordering, provenance round-trip across every observation, and the 404 envelope. 94 tests green; ruff + mypy clean. Phase 2 Task 2 deliverable is shippable.
2026-05-16 — Phase-gate sanity check (M0–M7) caught an ETag bug: meta.generated_at used datetime.now(), so the response bytes (and thus the ETag) changed on every request, defeating the PRD §5.7 If-None-Match contract. Fixed by deriving generated_at from max(document.authored_on) so the chart response is byte-stable until new data lands. tests/test_chart.py gained a stability test + an If-None-Match → 304 regression guard. Live stack re-verified: 9 observations round-trip provenance, ETag stable across requests, If-None-Match → 304, RFC 7807 + OperationOutcome envelopes correct, all 5 TJC gaps detected, completeness_score = 1.0. 95 tests green.
2026-05-16 — M8: /fhir/* search endpoints — GET /fhir/Patient/{id}, /fhir/DocumentReference (read + searchset w/ patient & type filters), /fhir/Observation (read + searchset w/ patient & code filters), /fhir/Provenance stub. New app/api/pagination.py with opaque base64 cursor (UUID-ordered, stateless) + Bundle.link self/next builder. Default _count per PRD §5.9 (DocRef=20, Obs=50, max=100). Each Bundle round-trips through fhir.resources.R4B.Bundle.model_validate. tests/test_fhir_search.py — 12 cases including paginated traversal asserting every row visited exactly once. 108 tests green.
2026-05-16 — M9: /fhir/Patient/{id}/$everything + /fhir/metadata — Synthea-shaped searchset Bundle (Patient → Encounters → DocumentReferences → ClinicalImpressions → Observations) via new build_everything_bundle; hand-built CapabilityStatement via new build_capability_statement (Patient w/ everything operation, DocumentReference + Observation w/ search-type + searchParam, Provenance w/ search-type). /fhir/metadata mounted on a dedicated auth-free sub-router (FHIR R4 spec requirement). tests/test_capability.py — 9 cases (CapabilityStatement validates, enumerates every resource, advertises $everything; $everything entry count matches expected breakdown, Patient ordered first). 116 tests green.
2026-05-16 — M10: custom Provenance extension + Provenance resources — PH_OFFSET_EXT URL constant (http://perspectiveshealth.ai/fhir/StructureDefinition/text-position-selector), to_fhir_provenance builds a complex extension carrying {start, end} valueInteger char offsets on Provenance.entity[0].extension. build_everything_bundle + build_patient_bundle both emit one Provenance per LOINC observation (total $everything entries: 30). New /fhir/Provenance search returns real resources, filterable by target=Observation/{id}. tests/test_provenance_extension.py — 8 cases including raw_text[start:end] cross-check against the relational ground truth. 123 tests green.
2026-05-16 — M11: response-level provenance round-trip — extended tests/test_provenance.py from the Phase 1 table-level walk to the Phase 2 wire-level invariant across /chart, /observations, /fhir/Observation, and $everything. A regression in any serializer is caught at the API boundary the reviewer actually calls. 127 tests green.
2026-05-16 — M12: README rewrite + Phase 2 sample JSON. Live-regenerated examples/marcus_reyes_chart.json (canonical /chart, 79KB) and examples/marcus_reyes_everything.json (FHIR $everything Bundle, 30 entries, 44KB). README leads with the "not a wrapper" framing, dual-surface diagram (Phase 1 substrate → /api/v1/* + /fhir/*), inlined /chart shape (~60 lines truncated), custom Provenance extension URL + rationale, and the updated submission contract (chart inline + Bundle attached). Phase 2 complete.
2026-05-16 — M13 (optional, Q1): synchronous FHIR Bulk Data $export stub — GET /fhir/Patient/{id}/$export streams the patient compartment as application/fhir+ndjson, one JSON-per-line, Content-Disposition: attachment. Same 30 resources as $everything (Patient+Encounter+DocRef+ClinImp+Obs+Provenance), framed as a stream not a Bundle. CapabilityStatement now advertises both `everything` and `export` operations on Patient. ETag middleware excludes NDJSON (already configured). tests/test_export_ndjson.py — 5 cases incl. per-line FHIR validation and count-parity with $everything. 132 tests green; live stack verified (30 lines, correct counts, attachment headers). Phase 2 fully complete with optional milestone shipped.
2026-05-16 — Phase 2 review pass (bugs, redundancy, comments). Real bugs fixed: (1) /chart meta.generated_at was naive, mislabelled vs FHIR datetimes — now stamped with America/Chicago to match _fhir_datetime; (2) FHIR DocumentReference.type was {"text": "soap"} while /chart loinc_type emitted full LOINC coding — both surfaces now share LOINC_DOCUMENT_TYPE in mappers.py and emit consistent coding + slug text; (3) build_capability_statement regenerated datetime.now() per request, breaking /fhir/metadata byte-stability — date now pinned at module load; (4) pagination._replace_query_param double-encoded existing query values via urlencode — switched to parse_qsl for correct round-trip. Redundancy removed: (5) three handlers (/fhir-bundle, $everything, $export) ran identical N+1 SELECTs per document — extracted to _load_patient_compartment with one IN-list query; (6) get_patient_observations and get_patient_asam_evidence had the same N+1 — collapsed to one IN-list each; (7) _parse_patient_filter and _parse_observation_target merged into _parse_reference(value, resource_type); (8) section enrichment loop duplicated across /intake and /chart — extracted _enrich_sections(document, statuses, with_provenance=...); (9) unused encounters query + `del encounters` removed from get_patient_chart; (10) hand-rolled model_dump in NDJSON path now uses _serialise. Comments: docstring updates removed stale "M9 will retire" notes; main.py docstring updated for current state; pool-doubling caveat added to audit_read; MVP-pagination limitation called out on _paginate; scaling note added to mappers module docstring. 132 tests green; sample JSONs regenerated.
2026-05-15 — Phase 3 kickoff: wrote documents/phase_3.md (playbook), phase_3_PRD.md (approved; Q1–Q7 resolved), and phase_3_implementation_plan.md (M0–M13). Phase 3 = cited-rationale clinical decision endpoints (POST /asam-loc + POST /tjc-audit) — deterministic ASAM Ch.10 rule engine + 13-EP TJC predicates, with Claude Sonnet 4.6 narrating via Anthropic Citations API + native Structured Outputs; assessments cached by (patient_id, evidence_hash, model_version).
2026-05-15 — M0 (Phase 3): added anthropic>=0.40 + tenacity>=8 deps; new ANTHROPIC_API_KEY + PHEALTH_LLM_MODEL settings (default claude-sonnet-4-6); scripts/smoke_anthropic.py one-shot Citations-API verifier (passing: 1 citation returned, ~0.002 USD); README "Phase 3 prerequisites" section. 132 tests green.
2026-05-15 — Project-wide OpenAI → Anthropic refactor: deleted app/ingest/embeddings.py + pgvector embedding column on clinical_document (Alembic revision phase3_000_drop_embedding, idempotent IF EXISTS); section_detector LLM fallback rewritten as Claude tool-use (single tools=[record_sections] forced tool_choice, preserves the str.find provenance recovery); removed openai dep, OPENAI_API_KEY + EMBEDDING_MODEL settings, and all OpenAI / pgvector mentions from README. Claude is now the single LLM provider for the project. 132 tests green; ruff + mypy clean.
2026-05-15 — M1 (Phase 3): three new SQLModel classes — AsamAssessment, TjcAuditResult, LlmInvocation (app/db/models.py) — and Alembic revision phase3_001_clinical_assessments creating asam_assessment + tjc_audit_result + llm_invocation tables. Both cache tables carry UNIQUE(patient_id, evidence_hash, model_version) to back the Phase 3 idempotency contract (phase_3_PRD.md §5.7). LlmInvocation has no FK to patient (audit-retained-after-delete). Autogen flagged clinical_document.fts as dropped (false positive — it's a Postgres GENERATED column) and that diff was hand-removed. Migration round-trips: upgrade → downgrade -1 → upgrade head all clean. tests/test_schema.py gained 5 cases: per-table round-trip, asam_assessment cache-key uniqueness, model-swap creates a new cache namespace. 137 tests green; ruff + mypy clean.
2026-05-15 — M2 (Phase 3): app/clinical/ package created (clinical/, clinical/asam/) with rubric.py — Dimension (IntEnum, 6 dims), Subdimension (StrEnum, 12 members), MinLoc (StrEnum, 13 levels 0.5-4.0-PSY incl. 3.7-BIO), RiskRating (StrEnum, 57 namespaced members — same bare token e.g. "3A" carries different meaning across subdimensions, so members are DIM1_W_3A vs DIM3_APS_3A_COE). MIN_LOC_BY_RATING covers all 57 ratings; COE_BEARING_RATINGS (Dim 3 APS COE family); BIO_BEARING_RATINGS (Dim 1/2 = 3B); VALID_RATINGS_BY_SUBDIM for runtime validation; MIN_LOC_DISPLAY for the API response. No ASAM rubric anchor text reproduced — paraphrased labels only, per ASAM IP boundary (phase_3_PRD.md §3). Marcus admission spot-checks pass: DIM1_W_3A → L3_7, DIM3_APS_1B → L1_7 non-COE, DIM4_RSU_D → L3_1. 137 tests green; ruff + mypy clean.
2026-05-15 — M3 (Phase 3): app/clinical/asam/risk_ratings.py — ClinicalSnapshot dataclass (loads ~4 SELECTs once per call; latest_observation picks by max(authored_on)) plus 12 per-subdimension helpers with explicit clinical reasoning (CIWA-Ar+concurrent dependence→DIM1_W_3A; chlordiazepoxide active→DIM1_AM_C; PHQ-9≥15+GAD-7≥10+passive ideation→DIM3_APS_1B; AUDIT-C≥8/DAST-10≥8→DIM4_RSU_E; etc). Carved out _interpret_cssrs to handle "no active SI/plan/intent" negation pattern after the naive substring check tripped on it. tests/clinical/test_risk_ratings.py: 17 cases — C-SSRS parser unit tests (4), per-helper snapshot tests (10), empty-patient defaults, live ingested Marcus → DIM1_AM=C + DIM3_APS=1B (non-COE) + no 3B (non-BIO). The demo-defining set: DIM1_AM_C → Min 3.7, all other dims below — Marcus will resolve to Level 3.7 non-COE non-BIO once M4 lands. 154 tests green; ruff + mypy clean.
2026-05-15 — M4 (Phase 3): app/clinical/asam/level_decision.py — ASAM 4th-ed Chapter 10 Determination Rules (pp. 279-281) as pure Python. LevelDecision Pydantic BaseModel (level, modifiers, co_occurring_enhanced, biomedical_enhanced, rules_fired) + decide(ratings). Cascade: L4 → 3.7-BIO+COE escalation → L4-PSY → medically managed (1.7/2.7/3.7/3.7-BIO) → clinically managed residential (3.1/3.5) → clinically managed outpatient (1.5/2.1/2.5) → below (1.0/0.5). COE applies "-COE" suffix and upgrades 3.1→3.5 / 2.1→2.5. Rubric tweak: DIM4_RSU_C / DIM4_RSB_C → L2_5 (was L2_1) per playbook §A3 — required for the day-8 step-down demo. tests/clinical/test_level_decision.py: 25 table-driven cases covering Marcus admission, day-8 simulated step-down, L4 / L4-PSY / 3.7-BIO escalations, COE suffix + upgrades, empty/all-zero defaults, Pydantic round-trip. tests/clinical/test_deterministic_core.py: end-to-end Marcus chart → compute_risk_ratings → decide → Level 3.7 non-COE non-BIO. **The deterministic core (M2+M3+M4) is now end-to-end functional with zero Claude calls.** 180 tests green; ruff + mypy clean.
2026-05-15 — M5 (Phase 3): app/clinical/tjc/ package — ep_catalog (13 EpDefinitions paraphrased from public R3 reports per phase_3_PRD.md §5.5), audit_functions (TjcSnapshot + 13 audit predicates split 8 TjcCoverage-backed / 5 raw-row / 1 conditional-applicability), runner (run_audit + summarize). G1-G5 wired via linked_planted_gap. Live Marcus chart produces the exact PRD §6.2 distribution: 13 findings, 5 gaps (all planted), 7 satisfied, 1 not_applicable (no OUD), 0 ambiguous. tests/clinical/test_audit_functions.py: 35 cases — per-EP unit tests + live-Marcus integration tests + per-gap linkage tests. **Bug-check fix during M5 close-out**: _finding_from_coverage was discarding Phase 1 citations on negative findings, but PRD §6.2 explicitly shows G1 carrying a context citation to the single PHQ-9 administration. Fixed: negative_finding flag now drives narration register only; context citations are preserved. 215 tests green; ruff + mypy clean. **The full deterministic core (M2-M5) is end-to-end functional — both ASAM Level recommendation AND TJC compliance audit run with zero Claude calls.**
2026-05-15 — M6 (Phase 3): app/clinical/llm/claude_client.py — single chokepoint for every Claude API call. ClaudeClient wraps anthropic.Anthropic (max_retries=3, timeout=60). messages_create uses with_raw_response.create to capture anthropic-ratelimit-* headers, computes xxh64 response_hash, times the call, and writes one LlmInvocation row in its own session (own-transaction so audit survives downstream rollback). Inline CircuitBreaker (~50 lines, deterministic clock injection for tests): closed → opens after 3 failures in 60s → half-open after 60s cooldown → closed on next success. Typed exceptions ClaudeRateLimited / ClaudeUnavailable / ClaudeBreakerOpen surface to the API layer for RFC 7807 mapping (phase_3_PRD.md §5.9). tests/clinical/test_claude_client.py — 11 cases via SDK-method mocking (zero live API calls): happy path + LlmInvocation row contents, RateLimit/APIStatus → typed exceptions with row logging, breaker opens after threshold, breaker recovers after cooldown, window-expiry semantics, cache-token capture, CircuitBreaker unit tests. 227 tests green; ruff + mypy clean.
2026-05-15 — M7 (Phase 3): three modules for the LLM-narration plumbing. (1) app/clinical/shared/evidence_retrieval.py — build_evidence_document/build_evidence_documents emits Anthropic Citations-API document blocks; embeds (doc_id, char_start, char_end) in context JSON; parse_document_context inverse. Generic CitationLike Protocol so AsamEvidence rows, EpFinding.evidence_pointers, and Phase 2 ProvenanceRead all flow through one helper. (2) app/clinical/llm/citation_validator.py — CitationValidator(db).validate(message, documents_sent, patient_id) walks every CitationCharLocation, parses context, fetches ClinicalDocument, verifies raw_text[char_start+offset:end] == cited_text. Returns ValidationResult with four typed failure modes: unknown_doc / out_of_range / snippet_mismatch / malformed_context. Aggregates all failures, not just first. (3) app/clinical/llm/structured_output.py — call_with_schema(client, response_model, ...) wraps ClaudeClient with forced tool-use (single submit_structured_response tool whose input_schema = response_model.model_json_schema()). Tool-use is the sanctioned fallback per PRD §5.6 since SDK 0.102.0 does not expose response_format; module-level docstring documents the migration path when SO ships. Typed StructuredOutputError raises preserve raw_payload for debugging. Tests: 19 cases via mocked SDK (zero live calls) — 6 evidence-retrieval shape tests, 8 validator failure-mode tests (incl. cross-patient document leak), 5 structured-output tests asserting forced-tool kwargs + Pydantic validation. 246 tests green; ruff + mypy clean.
2026-05-15 — M8 (Phase 3): ASAM narration wired end-to-end. Five files. (1) app/clinical/asam/schemas.py — Pydantic models split LLM-emitted (AsamRationale + DimensionRationale + SubdimensionRationale + Citation) from final API response (AsamAssessmentRead + DimensionRead + SubdimensionRead + Recommendation) per PRD §6.1. (2) Extended CitationValidator with validate_claims(claims, patient_id) for tool-use-emitted citations (playbook §D3 shape); existing message-walking path still serves Claude's native Citations API. (3) app/clinical/llm/prompts.py — shared XML-tagged fragments (ASAM_SYSTEM_ROLE, REFUSAL_CLAUSE, TOOL_OUTPUT_CLAUSE, xml() helper) per PRD §6.6 / playbook §C3. (4) app/clinical/asam/narration.py — narrate(client, db, patient_id, decision, ratings) → NarrationOutcome: builds AsamEvidence-rooted document blocks, calls Claude via call_with_schema(AsamRationale), validates each citation, retries once with a corrective prompt listing the broken citations, degrades on second failure (strips citations, marks rationale_status=degraded, tags warning). (5) app/clinical/shared/response_builder.py — build_asam_response(...) pure function joining LevelDecision + ratings + NarrationOutcome into AsamAssessmentRead. tests/clinical/test_asam_narration.py — 6 cases via mocked SDK (zero live calls): happy path, retry-recovers, retry-then-degrades, full Marcus pipeline → Level 3.7 with non-COE non-BIO recommendation in canonical PRD §6.1 shape, degraded propagation, NarrationOutcome serializability. 252 tests green; ruff + mypy clean. **End-to-end mocked-Claude pipeline now lands a valid AsamAssessmentRead.**
2026-05-15 — M9 (Phase 3): TJC narration wired end-to-end. (1) app/clinical/tjc/schemas.py — TjcAuditResponse / TjcFindingNarration (LLM tool_use schema) + TjcAuditRead / TjcFindingRead / TjcSummary (final API response per PRD §6.2). (2) app/clinical/tjc/narration.py — narrate_tjc(client, db, patient_id, findings) → NarrationOutcome: **batches all 13 EPs into one Claude call** (~$0.077 versus 13 individual calls). TJC_SYSTEM_ROLE adds the surveyor-RFI register; negative findings get explicit absence-language instructions. Retry-once + degraded paths reuse M8 machinery (the NarrationOutcome's rationale slot holds a TjcAuditResponse on this path — deliberate dataclass reuse documented in module docstring). (3) build_tjc_response in response_builder.py joins engine EpFinding list with LLM narration by ep_code; computes the satisfied/gap/n/a/ambiguous summary; falls back to engine evidence_pointers when the LLM emits no citations for a positive finding. (4) tests/clinical/test_tjc_narration.py — 7 cases via mocked SDK (zero live calls): happy path, empty findings edge case, retry-recovers, retry-then-degrades, full Marcus pipeline → 13 findings + 7-satisfied + 5-gap + 1-n/a + all 5 planted-gap linkages preserved, degraded status propagation, negative-finding citation rules. 259 tests green; ruff + mypy clean. **The full Claude-narration path is complete for both ASAM and TJC; M10 wires it into the HTTP endpoints.**
2026-05-15 — M10 (Phase 3) — **Task 3 acceptance milestone**: POST /api/v1/patients/{id}/asam-loc + POST /api/v1/patients/{id}/tjc-audit live, with caching, GETs, and 304s. (1) app/clinical/shared/evidence_hash.py — xxh64 over the canonical contributing-row PKs (AsamEvidence + ExtractedObservation for ASAM; TjcCoverage + ExtractedObservation + ClinicalDocument for TJC). (2) app/api/deps_clinical.py — lru_cached ClaudeClient singleton dependency (shared circuit breaker across requests). (3) app/api/asam_loc.py + app/api/tjc_audit.py — POST endpoints with cache lookup by (patient_id, evidence_hash, model_version) → 200 cached / 201 fresh; ETag W/"{hash}-{model}"; Location header; 422 RFC 7807 on no-evidence; 503 + Retry-After on ClaudeBreakerOpen / ClaudeRateLimited; 201 + x-rationale-status=unavailable + x-rule-engine-only=true headers on ClaudeUnavailable (degraded path preserves the engine's recommendation); 502 on malformed structured output. GET /api/v1/asam-assessments/{id} + GET /api/v1/tjc-audits/{id} with If-None-Match → 304. (4) Phase 2 ETagMiddleware patched to respect handler-set ETags (Phase 3 derives its ETag from the cache key, not the body hash). (5) docker-compose.yml passes ANTHROPIC_API_KEY + PHEALTH_LLM_MODEL into the app container. (6) tests/clinical/test_endpoints_asam.py + test_endpoints_tjc.py — 14 cases via mocked SDK covering happy path, cache-hit, 304, 422, 503, degraded 201, 404; LlmInvocation tests hardened to filter by patient_id (resilient to live-demo state pollution). **Live curl flow verified end-to-end against the docker stack**: POST asam-loc → 201 Level 3.7 (Marcus); POST again → 200 cached in 14ms with same ETag; GET with If-None-Match → 304; POST tjc-audit (23s, real Claude) → 5 planted gaps surface with real surveyor-RFI narrative ("Standard CTS.03.01.09: Not Met. Documentation review revealed..."). 273 tests green; ruff + mypy clean. **Phase 3 endpoints are now shippable for the assessment submission.**
2026-05-15 — M11 (Phase 3): FHIR mirrors for the Phase 3 endpoints. (1) app/fhir/mappers.py — to_clinical_impression(assessment, patient) + to_detected_issue(finding, audit, patient). Custom CodeSystem URLs: http://perspectiveshealth.ai/CodeSystem/{asam-level,tjc-ep}. R4 caveat caught + fixed: DetectedIssue.category is R5-only; removed the field; documentation calls out that the discrimination rides on the project-owned ``code.coding.system`` URL. ClinicalImpression.finding[].itemCodeableConcept carries the recommended level; investigation[] entries document each rule fired. (2) CapabilityStatement extended with both new resources + search params. (3) app/api/fhir.py — GET /fhir/ClinicalImpression (+ /{id}) and GET /fhir/DetectedIssue (+ /{id}). search_detected_issues filters to status=="gap" findings (FHIR DetectedIssue semantically marks a problem, not a confirmation). DetectedIssue id = ``{audit_id}-{ep_slug}`` is stable across cache hits. (4) tests/clinical/test_fhir_clinical_decision.py — 10 cases: pure-mapper unit tests (R4 round-trip via fhir.resources), severity collapse, CapabilityStatement enumeration, live ClinicalImpression / DetectedIssue search + read. tests/test_capability.py updated for the 2 new resource types. 283 tests green; ruff + mypy clean.
2026-05-15 — M12 (Phase 3): demo-flow consolidation. tests/clinical/test_demo_flow.py — 7 PRD §8 success-metric tests mapped 1:1 to M1, M3, M4, M5, M6, M7, M8, M9 (Marcus → Level 3.7, 5 planted gaps surface, citation round-trip against raw_text, cache hit + 304, FHIR validation, LlmInvocation row written). Registered the `manual` pytest marker in pyproject.toml: `pytest -m manual` opts into live Claude tests; CI default is `pytest -m "not manual"`. 289 tests green; 1 manual deselected; ruff + mypy clean.
2026-05-15 — M13 (Phase 3): submission artifacts + docs. (1) scripts/generate_sample_json.py — deterministic sample generator using TestClient + mocked Claude (Sonnet 4.6 is not bit-stable; this gives the README a stable, reviewable artifact). (2) examples/marcus_reyes_asam_admission.json — Level 3.7 ASAM response. (3) examples/marcus_reyes_tjc.json — 7-satisfied / 5-gap / 1-n/a TJC audit. (4) README new "Phase 3 — Clinical decision endpoints" section: deterministic-core-plus-LLM-narration thesis, "Why not LLM-only?" paragraph citing ASAM IP, endpoint contract, cost/latency/cache table, quick-start curl flow. Phase boundaries + Submission sections updated with all 4 sample artifacts. (5) docs/adr/003-llm-narration.md — one-page architectural decision record: context (FDA SaMD + reviewer transparency vs LLM jitter), decision (rule engine + Claude narration + cache as determinism boundary), consequences, alternatives. 289 tests green; ruff + mypy clean. **Phase 3 complete. Phase 3 PRD §8 success metrics all met.**
